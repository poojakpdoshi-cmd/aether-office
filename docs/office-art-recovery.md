# Office Art Recovery Record

The current 16:9 office background contained malformed Manager Cabin anatomy. A prior Owner-selected office asset remains available through managed project storage and contains a valid, naturally seated manager depiction. The map now uses a narrowly scoped, non-interactive recovery layer over the manager alone; the rest of the approved horizontal background and all interaction geometry remain unchanged.

The visible Discussion Room highlight frame and blue marker shapes have been disabled globally. The dedicated invisible `office-deep-discuss` hotspot remains present at the calibrated 16:9 room coordinates, preserving direct room interaction and the required text-free default launch.

Focused office regressions, TypeScript checking, and the production build passed after these changes.
